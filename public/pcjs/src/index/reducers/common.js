import * as types from '../constants/index.js';
let initialState;
initialState = {};
export default function common(state = initialState, action) {
    switch(action.type) {
        default:
            return state;
    }
};